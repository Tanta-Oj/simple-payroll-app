<% flash.each do |msg_type, msg| %>
  <% next if msg_type == "timeout" %>
  <div class="alert alert-<%= msg_type %>" role="alert" id="alert">
    <a href="#" class="close" data-dismiss="alert">×</a>
    <%= msg %>
  </div>
<% end %>